import React from 'react';
import '../styles/NewSubmission.css';

let NewSubmission = () => {
  return (
      <div className="new-submission-container">
      {
        alert("The NewSubmission Component rendered. Pretend like it was all pretty and stuff.")
      }
      </div>
    );
  }

export default NewSubmission;