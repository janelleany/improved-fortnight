import React from 'react';
import '../styles/NavBack.css';

let NavBack = () => {
  return (
      <div className="nav-back-container">
        <p>The NavBack Component has rendered inside the header as well.</p>
      </div>
    );
  }

export default NavBack;