import React from 'react';
import '../styles/NewSubmissionForm.css';

let NewSubmissionForm = () => {
  return (
      <div className="new-submission-container">
      {
        alert("The NewSubmissionForm Component rendered. Pretend like it was all pretty and stuff.")
      }
      </div>
    );
  }

export default NewSubmissionForm;